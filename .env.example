GOOGLE_CLIENT_ID=
# Optional: comma-separated allowlist (lowercase). Empty = any verified Google account.
ALLOWED_EMAILS=
# Optional: override auto-generated cookie secret in ~/.config/agent-deck/session-secret
SESSION_SECRET=
# Optional LAN bind for phone access (LaunchAgent / serve:lan also set this)
# HOST=0.0.0.0
# PORT=3847
